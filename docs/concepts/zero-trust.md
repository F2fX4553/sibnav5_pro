# Zero Trust Architecture

Sibna assumes the server is compromised.

1.  **No Plaintext**: Keys are never sent to the server.
2.  **No Metadata Trust**: The server's timestamp is not trusted for ordering if it conflicts with the Ratchet.
